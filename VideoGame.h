#pragma once

// pragma once: used to insure that we don't include the same header file twice
// in the same .cpp file

// Decleration
const char* RecommendMeAVideoGame(char videoGame);
const char* RecommendMeFood(char food);